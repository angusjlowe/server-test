{
  "name": "server-test",
  "version": "1.0.0",
  "description": "simple server test for functionality that may be needed at hackathons",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "insane knight",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^2.2.19"
  }
}
